#include <stdio.h>

int main() {
    int n, contador;
    scanf("%d",&n);
    for (contador=0; contador < n; contador = contador+1) {
        printf("Eu adoro programar\n");
    }
    return 0;
}